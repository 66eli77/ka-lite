var HelpDownloadingVideosModel = Backbone.Model.extend({
/*
	defaults: function() {
      return {
        line1: "empty line",
        line2: "empty line"
      };
    },

    initialize: function() {
      this.set("line1": {% trans "Help for downloading videos" %});
      this.set("line2": {% trans "Q: How do I bulk-download videos?" %});
    } 
*/
});